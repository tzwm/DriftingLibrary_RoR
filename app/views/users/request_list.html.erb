<div class="container">
  <% flash.each do |key, value| %>
    <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
  <div class="row">
    <div class="span8">
      <div class="content content_nav">
        <div class="content_navbar">
          <%= link_to "我的书架", User.find(params[:id]), class: 'content_navbar_tab' %>
          <a class="content_navbar_tab" href="#">想借</a>
          <a class="content_navbar_tab" href="#">已借</a>
          <a class="content_navbar_tab" href="#">可借</a>
          <%= link_to "借书请求", {controller: 'users', action: 'request_list'}, class: 'content_navbar_tab content_navbar_tab_current' %>
        </div>
        <div>
          <table class="table table-striped table-hover book_list2">
            <tr>
              <th>图书</th>
              <th>请求者</th>
              <th>请求日期</th>
            </tr>

            <%= get_request_list(params[:id]) %>
          </table>
        </div>
      </div>
    </div>
    
    <%= render 'users/user_right_column' %>

  </div>
</div>

